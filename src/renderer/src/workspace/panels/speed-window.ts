// SPEED_WINDOW_MS lives in the main process (src/main/provider/claude/transcript-speed.ts); the renderer
// only needs the human label. Keep them in sync if the window changes (60s).
export const SPEED_WINDOW_LABEL = '60s window'
